---
- name: A test playbook
  hosts: localhost
  tasks:
    - fail:
      when: fail_test_playbook is defined
    - copy:
        dest: %%TEST_FILE_PATH%%
        content: %%TEST_FILE_CONTENT%%
    - debug:
        msg: %%TEST_LOG_STRING%%
